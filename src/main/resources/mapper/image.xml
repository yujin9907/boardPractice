<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.demo.domain.image.ImageDao">

    <select id="findById" resultType="site.metacoding.demo.domain.image.Image">
        select * from image where id=#{id}
    </select>
    <insert id="save">
        INSERT INTO image(imageName, imagePath)
            VALUES (#{imageName}, #{imagePath})
    </insert>

</mapper>