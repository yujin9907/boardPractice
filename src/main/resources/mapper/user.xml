<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.demo.domain.user.UserDao">
    <select id="login" resultType="site.metacoding.demo.domain.user.User">
        SELECT * FROM user WHERE username=#{username} AND password=#{password}
    </select>
    <select id="findById" resultType="site.metacoding.demo.domain.user.User">
        select * from user where id=#{id}
    </select>
    <insert id="insert">
        INSERT INTO user(username, password, date)
        VALUE(#{username}, #{password}, now())
    </insert>

</mapper>